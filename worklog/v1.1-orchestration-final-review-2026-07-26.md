# v1.1.0 总调度最终审查 Worklog

## 审查范围

- `docs/v1.1.0/prd.md`
- `docs/v1.1.0/feasibility-review.md`
- `docs/v1.1.0/architecture-contract.md`
- `docs/v1.1.0/agent-execution-plan.md`
- `docs/v1.1.0/ux-design.md`
- 各 Agent 的 v1.1.0 worklog
- 后端、前端、Helm 和发布文档的当前工作区改动

## 总调度处理

1. 复核并冻结 Agent 文件边界，发生共享文件接线需求时先停止原负责人，再书面交接给新负责人。
2. 冻结日志采集兼容语义：
   - namespace 和 cluster 公共旧接口省略 `include_logs` 时保留 v1.0.0 行为；
   - v1.1.0 状态页显式关闭日志；
   - namespace batch 固定零日志；
   - Provider 内部默认零日志。
3. 要求日志范围上限来自持久化 `inspection_policy.max_log_pods`，默认 200，范围 1 至 1000。
4. 要求单 Pod 候选使用无巡检副作用的轻量 discovery API。
5. 要求 Issue、Run、Diagnosis、通知、历史 API 和 SQLite 共用递归脱敏与限长能力。
6. 将上述最终公开契约同步到 `docs/v1.1.0/architecture-contract.md`。

## 总调度独立验证

- 后端全量：`354 passed`，另有 1 条第三方 TestClient 弃用告警。
- 前端全量：14 个测试文件，`80 passed`。
- 前端生产构建：通过，75 个模块。
- 最终真实浏览器：
  - Settings 将 `max_log_pods` 从 200 调整为 1 后保存成功；
  - 6 Pod 范围在前端明确阻断，没有发送 namespace 巡检请求；
  - 单 Pod 下拉只发送轻量 discovery 请求，单 Pod 巡检仍可执行；
  - 状态页批量巡检调用独立状态入口；
  - 首轮发现 390px 视口存在 23px 横向溢出，退回前端修复后复验 `scrollWidth=clientWidth=390`；
  - 浏览器控制台无 error 或 warning。
- Python `compileall`：通过。
- Helm lint：通过，仅有 Chart icon 建议。
- Helm 单副本门禁：`replicaCount=2` 按预期拒绝渲染。
- Helm RBAC：只包含 `get/list`，Secret、ConfigMap、ServiceAccount 和 Pod Log 仅 `get`；无 `watch/create/update/patch/delete` 和 `pods/exec`。
- `git diff --check`：通过。

## 当前不能关闭的外部发布门禁

以下项目需要真实外部环境，当前工作区测试不能替代：

1. Kubernetes 1.34 和 1.36 的真实 E2E。
2. 真实 v1.0.0 PVC 的备份、升级、数据完整性校验和回退演练。
3. 代表性大集群的 API 调用量、日志读取量、采集字节数和耗时基线。

在这些记录补齐前，不把当前版本表述为“已可商用发布”。

## 提交状态

- 未创建 commit。
- 未执行发布、推送镜像或部署到生产。
