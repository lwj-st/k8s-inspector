# K8s Inspector v1.2.0 Release Notes

## 主要变化

- 页面降噪：移除默认页面中的口号式和重复说明文案，保留错误、风险和确认类提示。
- 问题工作台：默认只看开放问题，已恢复和已忽略通过筛选查看；表格列顺序、截断和状态颜色按排障优先级调整。
- 问题详情：首屏聚焦结论、资源、证据和建议动作；不再为无法确定资源的问题生成泛化命令；已忽略问题可恢复显示。
- 巡检结果：状态巡检、日志巡检和模板检查各自展示本模块结果；模板未命中不再作为默认噪音展示。
- 名称空间证据抽屉：优先显示 Pod 总数、异常 Pod、CPU 和内存摘要，正常对象和技术字段默认折叠。
- 视觉主题：全局改为浅色主题，左侧导航白底，主操作使用蓝色，危险操作使用红色。
- 用户菜单：左下用户卡片支持更改密码和注销，支持外部点击和 Esc 关闭。
- 访问路径：同一版本支持根路径 `/` 或子路径 `/inspector/` 部署，部署时二选一。
- Helm 和文档：新增根路径、子路径 values 示例，README 和 Chart 文档补充对应部署命令。

## 兼容性

- 不删除 v1.0.0 和 v1.1.0 已有 API 字段。
- 不改变已有问题编码语义。
- 不扩大 Kubernetes 写权限，仍保持只读巡检边界。
- 同实例同时支持 `/` 和 `/inspector/` 双入口不属于本版本 P0 范围。

## 验收

- 后端测试：`380 passed, 1 warning`。
- 前端测试：`94 passed`。
- 前端构建：通过。
- Helm 根路径和子路径 lint/template：通过。
- 真实 Kubernetes 1.34/1.36 集群 E2E 未在本地执行，不能声明真实集群验收通过。
